serenity {
  project.name = "Test Automation - Tricentis"
  take.screenshots = FOR_FAILURES

  webdriver {
    driver = chrome
    autodownload = true
  }
}

# En CI (GitHub Actions) debe ir =  -Dheadless.mode=true
headless.mode = false
